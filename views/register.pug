extends layout
block style_content
  link(rel='stylesheet' href='../public/stylesheets/register.css')
block content  
  // Movetru navbar
  nav.navbar.navbar-dark.bg-dark
    .container-fluid
      span.navbar-brand.mb-0.h1 Tekenable Test
      a.nav-item.nav-link(href='/') Login

  .container
    .row
      .register-form
        h1 Register your details in the form below
        p Username field accepts aplha numeric values only
        p user name must not already be  registered
        p The password must have a minimum length of 8 characters and contains at least 1 number, 1 uppercase and 1 lower case letter
        p You will get feedback when the username doesn't meet the criteria
        p Upon submission of a valid username and password, they are persisted to a database
        p You will get feedback that you have been registered
        form(action='registerUpdate' method='POST')
          input(type='text' name='usernameRegister' placeholder='Choose a Username' required='')
          input(type='password' name='passwordRegister' placeholder='Choose a Password' required='')
          input(type='submit')

 
  // Links for jQuery and Javascript file to interact with the static HTML and make dynamic
  script(src='//code.jquery.com/jquery-1.11.0.min.js')
  script(src='//code.jquery.com/jquery-migrate-1.2.1.min.js')
  script(src='http://cdnjs.cloudflare.com/ajax/libs/jquery-easing/1.3/jquery.easing.min.js')
  script(src='socket.io/socket.io.js')
  script(src='../public/javascripts/register.js')